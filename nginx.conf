events {}
http {
    server {
        listen 80;
        root /www/data;
        location / {
            autoindex on;
        }
        location /fonts/ {
            autoindex on;
        }
        location /images/ {
            autoindex on;
        }
    }
}
